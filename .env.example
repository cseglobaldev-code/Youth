# =============================================================================
# Y.O.U — Vite environment template
# =============================================================================
# Copy this file to `.env.local` and fill in real values. `.env.local` is
# gitignored. Anything starting with `VITE_` is exposed to the browser bundle,
# so NEVER put server-only secrets (APP_KEYS, JWT, Cloudinary, DB URL, etc.)
# in this file.
# =============================================================================

# Strapi REST base URL — no trailing slash.
# Use the same frontend origin when the Cloudflare CMS proxy is deployed.
# Local direct CMS: http://localhost:1337
# Cloudflare proxy: https://your-frontend-domain.tld
VITE_STRAPI_API_URL=http://localhost:1337

# Deprecated for production: VITE_* values are public in the browser bundle.
# Keep empty when using the Cloudflare Pages CMS proxy.
VITE_STRAPI_API_TOKEN=
